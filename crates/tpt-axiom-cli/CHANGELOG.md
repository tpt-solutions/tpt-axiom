# Changelog

All notable changes to `tpt-axiom-cli` are documented here.
The format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this crate adheres to [Semantic Versioning](https://semver.org/).

## [Unreleased]

### Changed

- Crate renamed from `axiom-cli` to `tpt-axiom-cli`. The installed binary
  name is unchanged (`axiom`).

## [0.1.0] - Phase 0

### Added

- `axiom` binary with `help`/`version`/`keys`/`verify` command surface.
  `keys` and `verify` are stubs pending Phase 3/4 work.
